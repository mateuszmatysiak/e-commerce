{
  "name": "@app/ui",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "pnpm typecheck && eslint --fix"
  },
  "dependencies": {
    "@app/tailwind-config": "workspace:*"
  },
  "eslintConfig": {
    "extends": [
      "@app/eslint-config/base"
    ]
  }
}
